<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <!-- This module was also published with a richer model, Gradle metadata,  -->
  <!-- which should be used instead. Do not delete the following line which  -->
  <!-- is to indicate to Gradle or any Gradle module metadata file consumer  -->
  <!-- that they should prefer consuming it instead. -->
  <!-- do_not_remove: published-with-gradle-metadata -->
  <modelVersion>10.4.3001</modelVersion>
  <groupId>com.dynamsoft</groupId>
  <artifactId>dynamsoftbarcodereaderbundle</artifactId>
  <version>10.4.3001</version>
  <packaging>aar</packaging>
  <dependencies>
    <!-- Dynamsoft License -->
    <dependency>
      <groupId>com.dynamsoft</groupId>
      <artifactId>dynamsoftlicense</artifactId>
      <version>3.4.40</version>
      <scope>compile</scope>
    </dependency>

    <!-- Dynamsoft Core -->
    <dependency>
      <groupId>com.dynamsoft</groupId>
      <artifactId>dynamsoftcore</artifactId>
      <version>3.4.30</version>
      <scope>compile</scope>
    </dependency>

    <!-- Dynamsoft Camera Enhancer -->
    <dependency>
      <groupId>com.dynamsoft</groupId>
      <artifactId>dynamsoftcameraenhancer</artifactId>
      <version>4.2.22</version>
      <scope>compile</scope>
    </dependency>

    <!-- Dynamsoft Capture Vision Router -->
    <dependency>
      <groupId>com.dynamsoft</groupId>
      <artifactId>dynamsoftcapturevisionrouter</artifactId>
      <version>2.4.30</version>
      <scope>compile</scope>
    </dependency>

    <!-- Dynamsoft Barcode Reader -->
    <dependency>
      <groupId>com.dynamsoft</groupId>
      <artifactId>dynamsoftbarcodereader</artifactId>
      <version>10.4.30</version>
      <scope>compile</scope>
    </dependency>

    <!-- Dynamsoft Image Processing -->
    <dependency>
      <groupId>com.dynamsoft</groupId>
      <artifactId>dynamsoftimageprocessing</artifactId>
      <version>2.4.31</version>
      <scope>compile</scope>
    </dependency>

    <!-- Dynamsoft Utility -->
    <dependency>
      <groupId>com.dynamsoft</groupId>
      <artifactId>dynamsoftutility</artifactId>
      <version>1.4.30</version>
      <scope>compile</scope>
    </dependency>

    <dependency>
      <groupId>androidx.appcompat</groupId>
      <artifactId>appcompat</artifactId>
      <version>1.6.1</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>com.google.android.material</groupId>
      <artifactId>material</artifactId>
      <version>1.9.0</version>
      <scope>runtime</scope>
    </dependency>
  </dependencies>
</project>
